# Runtime deps for the 8bit-pipeline container (TrueNAS deployment).
# Kept minimal — matches what scripts/podcast/*, scripts/social/*, scripts/watcher/* import.

requests>=2.32,<3
python-dotenv>=1.0,<2

# Video pipeline
faster-whisper>=1.0,<2
# whisperX: forced word alignment via wav2vec2 — refines Whisper word timestamps
# from ±300ms drift to ±50ms. Used by transcribe.py post-Whisper. Caption sync
# is now correct at the source instead of being rescued downstream by Gate 2.
# ~500MB wav2vec2 model is downloaded on first run, cached in /app/.cache.
whisperx>=3.1,<4
Pillow>=11.0,<12
# OpenCV (headless, no GUI deps) — face detection for the vertical 9:16 crop
opencv-python-headless>=4.10,<5

# LLM for clip picking + metadata generation
anthropic>=0.42,<1

# YouTube uploads
google-api-python-client>=2.150,<3
google-auth-oauthlib>=1.2,<2
google-auth-httplib2>=0.2,<1
