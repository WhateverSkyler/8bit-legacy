# EOD 2026-05-15

## Running unattended overnight on TrueNAS

**Rescan PID 1411444 + watcher PID 1416617** — full hardened-matcher PriceCharting rescan of all 6,121 retro game products. Started 15:09 ET, ETA ~22:00 ET (~7hr because the new strict matcher does more PC queries per product). When rescan exits, watcher auto-runs the audit + dry-run apply and writes review-queue CSVs to `~/8bit-pricing/data/logs/`.

**No auto-apply to Shopify overnight.** When user pings tomorrow asking about pricing, Claude reviews the classification, spot-checks samples, and (if clean) runs `python3 scripts/apply-truth-set.py --apply --skip-breaker-check` to push the changes live.

Check progress:
```bash
ssh -i ~/.ssh/id_ed25519 truenas_admin@192.168.4.2 'ps -p 1411444 2>/dev/null && echo running || echo DONE; wc -l ~/8bit-pricing/data/logs/search-refresh-20260515_121021.csv'
```

---

## What got shipped today

### eBay tax exemption — MTC resubmitted
- 07:50 ET: eBay rejected MTC cert ("GA registration number in GA column only") + flagged missing business name on `circuitswapllc` account.
- 13:00–14:00 ET cowork: regenerated MTC via `scripts/fix-mtc-ga-only.py` (GA-only, verified 1 non-empty state cell). User signed + re-uploaded. Business name `8-Bit Legacy, LLC / 103 Dogleg Dr, Moultrie, GA 31788-1361` added as additional shipping address on eBay (the path eBay's email explicitly authorized — owner-name field is person-only).
- SST cert was NOT flagged in this round. Still in original review.
- Verdict expected by **~2026-05-22**.

### Pricing automation — full reset in progress
Past PriceCharting runs actively CORRUPTED prices (Wii Party U $60 → $12 etc.) because the matcher confidently mismatched across console boundaries. Fixed at the root today:

- **NEW** `scripts/_console_taxonomy.py` — canonical console enum, STRICT lookup, no `in`/`contains` fuzzy. `same_console("Wii", "wii u")` returns False by construction.
- **PATCHED** `scripts/search-price-refresh.py` — uses the taxonomy; raised similarity floor 0.3 → 0.55; added ambiguity-gap rejection (top vs second-best within 0.10 = reject).
- **PATCHED** `dashboard/src/lib/matching.ts` — deleted Tier-4 fuzzy substring block + removed `"fuzzy"` from MatchType union.
- **NEW** `scripts/apply-truth-set.py` — canonical applier with breaker check, 3× ratio guards both directions, title-corruption sniffer, live-Shopify-price re-fetch before each write, 50-fail kill switch.
- **NEW** `scripts/auto-finish-rescan.sh` — the watcher running on NAS right now.

**Kill switch (defense in depth, all three active):**
1. `config/pricing.json` → `auto_apply_enabled: false`
2. Circuit breaker `pricing` tripped in `dashboard/db/8bitlegacy.db` settings table
3. Both `price-sync` and `pokemon-price-sync` cron jobs set to `enabled: false` in `dashboard/src/lib/jobs.ts`

Full plan + memory: `~/.claude/plans/luminous-inventing-biscuit.md`, `memory/project_pricing_reset_2026-05-15.md`.

### Ads campaign — partial cleanup, blocked on Google quota
- Verified campaign **STILL PAUSED**, $0 spend in 10 days (no leak since 5/05).
- **Conversion pixel `AW-18056461576` verified live on storefront** — was 14 days stale. Page View action shows 90 conv in last 30d → pixel firing on organic traffic.
- Refreshed `data/cib-offer-ids.txt` — 6,112 IDs, no new CIB variants since prior snapshot.
- **NEW** `scripts/ads_rebuild_cib_tree.py` — minimal wrapper for tree-only rebuild (avoids classifier false positives on full `ads_launch.py`).
- Started full CIB tree rebuild. Tore down 2,009 old nodes + created 5 skeleton nodes. **Hit Google Ads daily mutation quota** before pushing the 6,112 item_id negatives.

**Tree is currently EMPTY of CIB negatives.** Safe because campaign is paused, but cannot be enabled until rebuild finishes.

Quota resets ~tomorrow 10am ET. Rebuild takes ~5 min once unblocked.

---

## Tomorrow morning checklist

When user pings about any of: pricing, audit, ads, "is it ready", "review it", "go ahead":

### 1. Pricing reset (rescan should be done by ~22:00 ET tonight)
```bash
ssh -i ~/.ssh/id_ed25519 truenas_admin@192.168.4.2 'ls -la ~/8bit-pricing/data/logs/auto-finish-*.log | tail -1; tail -60 ~/8bit-pricing/data/logs/auto-finish-*.log | tail -60'
```
- Review CLASSIFICATION SUMMARY counts
- Sample 30 APPLY rows across consoles (Wii, NES, SNES, PS2, GameCube). Verify each title matches sensibly and target = `market × correct multiplier × $X.99`
- Verify Wii Party U + Wii Sports Resort specifically (the regression canaries)
- If clean: `python3 scripts/apply-truth-set.py --csv $(ls -t ~/8bit-pricing/data/logs/search-refresh-2026*.csv | head -1) --apply --skip-breaker-check`
- After apply: reset pricing breaker via `sqlite3 dashboard/db/8bitlegacy.db "UPDATE settings SET value='{\"tripped\":false,\"reason\":\"\",\"trippedAt\":\"\"}' WHERE key='circuit_breaker_pricing';"`

### 2. Ads CIB rebuild (after Google quota resets ~10am ET)
```bash
python3 scripts/ads_rebuild_cib_tree.py
python3 scripts/verify_cib_exclusion.py   # MUST show 3/3 PASS
curl -s https://8bitlegacy.com/ | grep -oE 'AW-[0-9]+/?[A-Za-z0-9_-]*' | sort -u   # AW-18056461576 expected
```

### 3. Re-enable Pokemon scheduler (lower risk than retro)
- `dashboard/src/lib/jobs.ts` → `pokemon-price-sync.enabled: true`

### 4. Campaign relaunch ready when:
- Pricing reset applied ✅
- CIB verifier 3/3 PASS ✅
- Pixel still verified ✅
- Both circuit breakers reset

User flips campaign to ENABLED via Google Ads UI. Watch first hour with `python3 scripts/ads_daily_report.py --since today`.

---

## Other state (not blocking)

| Item | Status |
|---|---|
| eBay tax cert verification | Watch inbox ~5/22 |
| Merchant Center "Missing local inventory" | Still ticking down per 5/14 fix |
| Podcast pipeline | Different session handling — not in scope here |
| YT OAuth keep-alive | TrueNAS cron id=734 daily 3am ET (per `project_yt_oauth_state.md`) |

---

## Memory pointers

- `project_pricing_reset_2026-05-15.md` — full pickup playbook for the pricing reset
- `project_ads_launch_state.md` — full ad campaign state + tomorrow's morning rebuild plan
- `project_ebay_resale_exemption.md` — eBay tax cert status (5/22 verdict expected)
- `feedback_pricing_automation_regressed.md` — ⚠️ permanent reminder that PriceCharting automation is a regression source, not a refresh tool

`MEMORY.md` index up to date.

---

## PM session (desktop, ~12:00 PM → 6:45 PM ET)

Switched from the Mac to the Linux desktop. Spent the afternoon on the
shorts pipeline. Two architecture rebuilds before landing on the right one.

### Shorts pipeline — third architecture wins

**Greenfield rebuild attempt (committed `fe7e0b4`, **reverted** `6d9baba`)**
- Replaced 6-layer gauntlet with one Sonnet call per topic file using
  audio-silence breakpoints as structural truth.
- Tested on April 19 (15 clips, 40% sweet spot, $0.18/episode).
- Tested on May 5 — produced 22 rendered clips. User watched them and
  they STILL sucked: mid-sentence ends, weak openings, multi-topic sprawl,
  captions out of sync. Rejected.
- Reverted everything. Old `pick_clips.py` gauntlet is back, untouched
  and still callable.

**Custom human-in-the-loop editor (the actual answer)** — committed `7bf4b25`:
- `scripts/podcast/clips/propose.py` — chunked Sonnet calls (15-min slices,
  30s overlap) for even coverage of long episodes. May 5 → 106 proposals
  across the full 67 min for $0.26.
- `scripts/podcast/clips/review_server.py` — Flask backend with state
  persistence and range-supported audio playback.
- `scripts/podcast/clips/static/{index.html,app.js,style.css}` — vanilla
  JS editor: full transcript with clip overlays, draggable handles snapping
  to word boundaries, click-any-word to seek+play, live drag tooltip with
  new duration, "+ New Clip" by clicking start word then end word, auto-save.
- `scripts/podcast/clips/review_cli.py` — orchestrator: proposes, starts
  server, opens browser, blocks until Submit.
- `scripts/podcast/clips/fetch_from_nas.py` — rsyncs episode files from
  NAS to /tmp.

**Real-content end-to-end on May 5: NOT yet validated.** Server kept
dying during testing (background-task killing issues), audio playback
broke at one point, switched to laptop before completing a real review.
**Pick up tomorrow per `docs/clip-review-handoff-2026-05-15.md`**.

### Side issues addressed
- **Google Voice spam** (calls from area code 229) — diagnosed as neighbor
  spoofing. Told user to enable Filter Spam in voice.google.com → Settings
  → Calls.
- **Strategic ads agent** still firing 3x/day on cron with "no change."
  Worth dropping cadence to 1x/week or pausing until ads is re-enabled.

### Open loops carried into tomorrow

| Item | Where to pick up |
|---|---|
| Validate clip review editor on May 5 | Laptop. See `docs/clip-review-handoff-2026-05-15.md` |
| eBay MTC verdict | Email by ~2026-05-22; nothing to do until then |
| CIB ad-tree rebuild + campaign launch | Whenever ads come off hold |
| Strategic-ads agent cadence | Pause or drop to weekly |

### Honest reflection

Two architecture rebuilds before the right one. The pattern was: both
rebuilds tried to make AI do the editorial work autonomously. Both
failed. The third approach (you do the editorial work, AI proposes
options) is the correct decomposition — it just took two rounds of
chasing the wrong abstraction to get there.

The editor itself works mechanically (server, API, drag, click — all
proven in isolated tests). It hasn't been used end-to-end on real
content for a real review session yet. That's tomorrow.

Repo state: latest commit `b991013`, all pushed to origin/main.

Sleep well.
